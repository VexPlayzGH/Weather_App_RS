[package]
name = "weather_app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {version = "1.0.228", features = ["derive"]}
reqwest = {version = "0.13.4", features = ["blocking", "json"]}
serde_json = "1.0.150"
colored = "3.1.1"
