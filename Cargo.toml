[package]
name = "weather_app"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = {version = "1.0.216", features = ["derive"]}
reqwest = {version = "0.12.9", features = ["blocking", "json"]}
serde_json = "1.0.134"
colored = "2.2.0"
